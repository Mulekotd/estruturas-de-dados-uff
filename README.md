# Estruturas de Dados e Seus Algoritimos

## Conteúdo Programático

```txt
Árvores (Binária, Binária de Busca e AVL), Grafos, Ordenação externa de arquivos binário e texto (geração de partições classificadas e intercalação de partições). Tabelas hash (em memória principal e secundária). Árvores B e B+ (em memória principal e secundária). Heaps (em memória principal e secundária).
```
